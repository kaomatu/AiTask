rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ログインしたユーザー自身のみ、自分のユーザードキュメント以下を読み書き可能にする
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
